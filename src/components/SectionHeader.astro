---
interface Props {
	title: string;
	viewAllLink?: string;
}

const { title, viewAllLink } = Astro.props;
---

<div class="section-header-wrapper">
	<h2 class="section-title">{title}</h2>
	{viewAllLink && (
		<a href={viewAllLink} class="view-all-link">View All →</a>
	)}
</div>

<style>
	.section-header-wrapper {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 2rem;
	}

	/* Matches the site-wide section heading in components.css:
	   left aligned, accent rule underneath. */
	.section-title {
		font-size: var(--text-2xl);
		color: var(--gray-0);
		margin: 0;
		padding-bottom: 0.5rem;
		border-bottom: 2px solid var(--accent-regular);
		flex-grow: 1;
	}

	.view-all-link {
		color: var(--accent-regular);
		text-decoration: none;
		font-size: var(--text-base);
		font-weight: 500;
		transition:
			color var(--theme-transition),
			background-size var(--sweep);
	}

	.view-all-link:hover {
		color: var(--accent-dark);
	}

	@media (min-width: 50em) {
		.section-title {
			font-size: var(--text-3xl);
		}
	}
</style>
